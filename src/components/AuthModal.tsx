import { useState } from 'react'
import { useAuth } from '../contexts/AuthContext'
export default function AuthModal({ open, onClose }: { open: boolean; onClose: () => void }) {
  const { signInWithGoogle, signInWithMagicLink } = useAuth(); const [email, setEmail] = useState(''); const [message, setMessage] = useState<string | null>(null); if (!open) return null
  const magic = async () => { try { await signInWithMagicLink(email); setMessage('Check your email for a secure sign-in link.') } catch (error) { setMessage(error instanceof Error ? error.message : 'Sign-in is unavailable.') } }
  const google = async () => { try { await signInWithGoogle() } catch (error) { setMessage(error instanceof Error ? error.message : 'Google sign-in is unavailable.') } }
  return <div className="fixed inset-0 z-[70] flex items-center justify-center bg-stone-950/60 p-4 backdrop-blur-sm" role="dialog" aria-modal="true"><section className="w-full max-w-md rounded-[2rem] bg-white p-6 shadow-2xl"><div className="flex justify-between gap-3"><div><p className="text-xs font-semibold uppercase tracking-[.22em] text-[#8b5e3c]">Northstar account</p><h2 className="mt-2 text-2xl font-semibold text-stone-900">Sign in or register</h2></div><button type="button" onClick={onClose}>Close</button></div><p className="mt-3 text-sm leading-6 text-stone-600">Save reports, manage subscriptions, and access your workspaces.</p><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@company.com" className="mt-5 w-full rounded-xl border border-stone-200 p-3" /><button type="button" onClick={() => void magic()} disabled={!email} className="mt-3 w-full rounded-full bg-stone-900 px-4 py-3 text-sm font-semibold text-white disabled:opacity-50">Email me a magic link</button><button type="button" onClick={() => void google()} className="mt-3 w-full rounded-full border border-stone-300 px-4 py-3 text-sm font-semibold text-stone-800">Continue with Google</button>{message ? <p className="mt-4 text-sm text-stone-600">{message}</p> : null}</section></div>
}
